name: Linux

on:
  workflow_call:
    inputs:
      repository:
        description: Public repository to verify
        required: false
        type: string
      ref:
        description: Git ref to verify
        required: false
        type: string
      verify_source:
        description: Run Verify from source
        required: false
        type: boolean
        default: false
  workflow_dispatch:
    inputs:
      repository:
        description: Repository to verify
        required: true
        type: string
      ref:
        description: Git ref to verify
        required: true
        type: string
      verify_source:
        description: Run Verify from source
        required: false
        type: boolean
        default: false

permissions: {}

concurrency:
  group: linux-${{ github.workflow }}-${{ github.event_name }}-${{ inputs.repository || github.repository }}-${{ inputs.ref || github.ref }}
  cancel-in-progress: true

env:
  GOWORK: off

jobs:
  linux:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - {name: Ubuntu 22.04 AMD64, runner: ubuntu-22.04}
          - {name: Ubuntu 24.04 AMD64, runner: ubuntu-24.04}
          - {name: Ubuntu 22.04 ARM64, runner: ubuntu-22.04-arm}
          - {name: Ubuntu 24.04 ARM64, runner: ubuntu-24.04-arm}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || github.ref }}
          persist-credentials: false

      - name: Install Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: false

      - name: Test
        if: ${{ !inputs.verify_source }}
        run: go -C tools tool secverify --root .. test

      - name: Test with Verify source
        if: ${{ inputs.verify_source }}
        run: go run -a -buildvcs=true ./cmd/secverify test
